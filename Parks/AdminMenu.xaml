<Window x:Class="Parks.AdminMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Parks"
        mc:Ignorable="d"
        Title="AdminMenu" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1">
            <StackPanel Height="41">
                <Button x:Name="btn_ready" Content="Добавить растение" Padding="20,5,20,5"  Click="btn_ready_Click" Margin="0,0,0,97" Width="243" HorizontalAlignment="Left" FontSize="20"/>
            </StackPanel>
            <StackPanel>
                <Button x:Name="btn_orderProd" Content="Назначить работника" Padding="20,5,20,5" Click="btn_orderProd_Click" Width="243" HorizontalAlignment="Left" FontSize="20"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
